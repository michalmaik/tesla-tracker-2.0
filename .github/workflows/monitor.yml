name: Tesla CPO Monitor

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests curl_cffi

      - name: Download previous state
        uses: actions/download-artifact@v4
        with:
          name: cars-state
          path: .
        continue-on-error: true

      - name: Run monitor
        env:
          MONITOR_MODE: CPO
        run: python monitor.py

      - name: Save state
        uses: actions/upload-artifact@v4
        with:
          name: cars-state
          path: |
            cars_state_cpo.json
            price_history_cpo.json
          retention-days: 30
          overwrite: true
